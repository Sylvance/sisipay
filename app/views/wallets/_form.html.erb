<%= form_with(model: wallet) do |form| %>
  <% if wallet.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(wallet.errors.count, "error") %> prohibited this wallet from being saved:</h2>

      <ul>
        <% wallet.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :amount, style: "display: block" %>
    <%= form.number_field :amount %>
  </div>

  <div>
    <%= form.label :sender_token, style: "display: block" %>
    <%= form.text_field :sender_token %>
  </div>

  <div>
    <%= form.label :recipient_token, style: "display: block" %>
    <%= form.text_field :recipient_token %>
  </div>

  <div>
    <%= form.label :checkout_url, style: "display: block" %>
    <%= form.text_field :checkout_url %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
